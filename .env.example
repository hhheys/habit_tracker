SERVER_HOST=localhost
SERVER_PORT=8080

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=habit_tracker
POSTGRES_SSL_MODE=disable

JWT_SECRET=secret-change-me-in-production

KAFKA_BROKERS=localhost:9092
KAFKA_OUTBOX_TOPIC=habit-tracker.events
OUTBOX_PUBLISH_INTERVAL=5s

PG_CONNECTION_STRING=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable
